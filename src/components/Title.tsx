import React from "react";
import "../styles/title-style.css";

const Title = () => {
  return (
    <div>
      <div className="title">Img2LaTeX Converter</div>
      <p>画像から数式を認識してTexのコードへ変換します。</p>
    </div>
  );
};

export default Title;
